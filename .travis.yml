sudo: required

services:
 - docker

env:
  matrix:
    - ARCH=s390x
    - ARCH=amd64
before_install:
    - docker build --compress -t dmkif/gotty-"${ARCH}" -f Dockerfile."${ARCH}" .

script:
   - docker images dmkif/gotty-"${ARCH}"
